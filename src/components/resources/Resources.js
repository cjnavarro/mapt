import React from 'react';
import Navigation from '../common/Navigation';
import ResourcesContainer from '../../containers/ResourcesContainer';

const Resources = () => (
  <Navigation displayName="Resources">
    <ResourcesContainer/>
  </Navigation>
);

export default Resources;
